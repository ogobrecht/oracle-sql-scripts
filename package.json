{
  "name": "oracle_sql_scripts",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/ogobrecht/oracle-sql-scripts"
  },
  "scripts": {
    "deploy": "cd examples && echo exit | sqlplus -S /@cloud_playground_data @deploy.sql && cd ..",
    "watch": "chokidar scripts/* examples/* --initial -c \"npm run deploy\""
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0"
  }
}
