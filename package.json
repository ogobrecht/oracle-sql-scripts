{
  "name": "oracle_sql_scripts",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/ogobrecht/oracle-sql-scripts"
  },
  "scripts": {
    "deploy": "echo exit | sqlplus -S /@playground @deploy_example.sql",
    "watch": "chokidar app_backend/* scripts/* --initial -c \"npm run deploy\""
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0"
  }
}
